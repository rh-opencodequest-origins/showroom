
:imagesdir: ../../assets/images

= {product_name_rhdh} Deployment

include::../style.adoc[]

= First step in Red Hat Developer Hub

== *Pick the chosen one* ! 


IMPORTANT: This is a *Shared part*. It means, *one person* will be able to go through those steps. The work accomplished will be useful for everyone!


image:m2/pe-user-picking.png[Empty {product_name_rhdh} Catalog,link=self,window=blank]

* User1 is : *{user1}*
* User2 is : *{user2}*
* User3 is : *{user3}*



== Deploying {product_name_rhdh}

Platform Engineers can deploy {product_name_rhdh} on OpenShift using the Operator or Helm Chart. Both of these installation methods are outlined in the https://docs.redhat.com/en/documentation/red_hat_developer_hub/1.6/html/installing_red_hat_developer_hub_on_openshift_container_platform/index#assembly-install-rhdh-ocp-helm[{product_name_rhdh} documentation^, window="content"]. In this lab, you’ll use the Helm Chart to deploy and manage your instance of {product_name_rhdh}. The source code for this Helm Chart can be found at https://github.com/openshift-helm-charts/charts/tree/main/charts/redhat/redhat/redhat-developer-hub[openshift-helm-charts repository on GitHub^, window="content"].

== Using GitOps to Manage {product_name_rhdh}

The deployment of {product_name_rhdh} is kept up to date using a GitOps workflow, as illustrated below.

image:m2/gitops-helm.png[GitOps Architecture Overview, width=80%]


.Curious about GitOps deployment ? 

[%collapsible]
====


The instance of OpenShift Container Platform used in this workshop environment has been preconfigured with OpenShift GitOps (Argo CD). 


image:m2/gitops-helm.png[GitOps Architecture Overview, width=80%]

Since this isn’t a GitOps-focused workshop, we’ve set up the basic GitOps workflow ahead of time. Specifically, we’ve pre-created a https://openshift-gitops-server-openshift-gitops.{openshift_cluster_ingress_domain}/applications/rhdh-{team-id}[rhdh-bootstrap Application in OpenShift GitOps, window="argo"] - you can view this by clicking the link and "log in via OpenShift" as 
*`{pe-username}/`{common_password}`*

NOTE: https://www.redhat.com/en/topics/devops/what-is-gitops[GitOps] is the practice of automating application deployment using Git repositories as the "source of truth" for an https://www.redhat.com/en/topics/automation/what-is-infrastructure-as-code-iac[infrastructure as code (IaC)] for the deployment configuration. The Git repository contains declarative configuration, typically in YAML format, that describes the desired deployment state, and a GitOps tool such as OpenShift GitOps ensures the application is deployed according to the configuration. Drifts from the configuration can automatically be patched.


image:m2/argocd-login.png[GitOps Architecture Overview, width=80%]



The  Argo CD Application creates Secrets, ConfigMaps, and another Argo CD Application named https://openshift-gitops-server-openshift-gitops.{openshift_cluster_ingress_domain}/applications/rhdh-{team-id}-bootstrap[]. The https://openshift-gitops-server-openshift-gitops.{openshift_cluster_ingress_domain}/applications/applications/rhdh-{team-id}[ArgoCD, window="argo"] Argo CD Application deploys {product_name_rhdh} using the Helm Chart. The configuration values passed to the Helm Chart are sourced from the https://gitlab-gitlab.{openshift_cluster_ingress_domain}/pe{team-id}/developer-hub-config/-/blob/main/values.yaml[rhdh/developer-hub-config/values.yaml file in GitLab, window="gitlab"]. OpenShift GitOps will detect changes to this file and redeploy {product_name_rhdh} in response. You can see these two sources in the *Details* tab of the `backstage` Application as shown.

image:m2/gitops-sources.png[GitOps Sources for the Developer Hub Application]



====

== Understand how GitOps is Working 

The starting point is the installation of the Internal Developer Portal, Red Hat Developer Hub. 
All resources has been set to let the magic of GitOps run the installation.
For the purpose of the workshop, you will do a minor change to trigger the installation.


*Update the App Title of {product_name_rhdh}*

The configuration file of the Internal Developer Platform is located in a Gitlab repo.

=== ✍️ Time to pratice ✍️ 


. Open this https://gitlab-gitlab.{openshift_cluster_ingress_domain}/pe{team-id}/developer-hub-config/-/blob/main/values.yaml[link, window="gitlab"]

image:m2/gitlab-login.png[]

. When prompted, log in as a Platform Engineer with 
* Username or primary email : `{pe-username}`
* Password :`{common_password}`


Remember, it's the same user, the platform engineer you have chosen who is performing actions also on Gitlab !

. Select *Edit > Edit single file*. 


. Find the YAML surrounded by `--- APP TITLE ---` and uncomment it by highlighting it and pressing `CMD + /` or `CTRL + /`.

[NOTE]
====
If the keyboard shortcut to uncomment through the web browser doesn't work , edit the piece of code in your local IDE and paste after ! 
====


image:m2/gitlab-change-app-title.png[Customizing the Title]

The uncommented part should look like :

image:m2/appConfig-title.png[]

. Scroll down and enter a commit message `feat: change application title`.
. Click the *Commit changes* button.


== Verify Internal Developer Portal is deployed

Let's ensure that a new deployment of {product_name_rhdh} is triggered and your new title is applied.

=== ✍️ Time to pratice ✍️ 

2 ways to do it:

* Wait few minutes to let our GitOps service observes the change and trigger the installation 
* Trigger the refresh by yourself following those instructions:

. Go to the https://openshift-gitops-server-openshift-gitops.{openshift_cluster_ingress_domain}/applications/rhdh-{team-id}[Developer Hub Application in Argo CD, window="argo"]

image:m2/argocd-login.png[]

. Click on Login via OpenShift 
image:m2/ocp-sso-login.png[]

. Click on rhsso 
. When prompted, log in as a Platform Engineer with 
* Username or primary email : `{pe-username}`
* Password :`{common_password}`

+
. Depending on your timing, it might already be progressing the latest sync, but if not, click the *Refresh* button. When the sync is in progress, you'll see a new Pod starting - this Pod will run the latest version of your configuration.


image:m2/rhdh-new-pod.png[{product_name_rhdh} with a Custom Title]

. Once the new Pod has started, visit your https://backstage-rhdh-{team-id}.{openshift_cluster_ingress_domain}[{product_name_rhdh} instance, window="rhdh"]. You should see the new title *PE Developer Hub* in the page header.
+
image:m2/rhdh-title.png[{product_name_rhdh} with a Custom Title]

[WARNING] 
====
If the pod is ready but there is no change on Developer Hub UI,
====
.Get help here 
[%collapsible]
====
include::../debug-pod-placement.adoc[]
====

Perfect ! The Internal Developer Portal 
