:imagesdir: ../../assets/images

= First step in Red Hat Developer Hub

== *Pick the chosen one* ! 


IMPORTANT: This is a *Shared part*. It means, *one person* will be able to go through those steps. The work accomplished will be useful for everyone!


image:m2/pe-user-picking.png[Empty {product_name_rhdh} Catalog,link=self,window=blank]

* User1 is : *{user1}*
* User2 is : *{user2}*
* User3 is : *{user3}*


== Activity: Access {product_name_rhdh}

include::../style.adoc[]

Once you have chosen a user from the team, go ahead!
{product_name_rhdh} has been pre-deployed with a base configuration in your workshop environment. You can find and access your instance in the https://console-openshift-console.{openshift_cluster_ingress_domain}/topology/ns/rhdh-{team-id}[backstage project on OpenShift^, window="console"].

Log in to {product_name_rhdh}:

. Visit the https://console-openshift-console.{openshift_cluster_ingress_domain}/topology/ns/rhdh-{team-id}?view=graph[backstage project^, window="console"] in your OpenShift cluster. You can log in with the platform engineer user chosen. Take the user with the *pe* suffix for instance superman-*pe* and the password /*{common_password}*

. Choose  *rhsso* as the identity provider

image:m2/ocp-sso-login.png[Choose rhsso as identity provider on OpenShift login,width=60%,link=self,window=blank]
. Return to the https://openshift-gitops-server-openshift-gitops.{openshift_cluster_ingress_domain}/applications/rhdh-{team-id}[`RHDH` Application in OpenShift GitOps^, window="argo"]. Depending on your timing, it might already be progressing the latest sync, but if not, click the *Refresh* button. When the sync is in progress, you'll see a new Pod starting - this Pod will run the latest version of your configuration.
+
image:m2/rhdh-new-pod.png[{product_name_rhdh} with a Custom Title]

. Select the *rhdh-{team-id}* Deployment in the Topology View.
+
image::m2/openshift-backstage-deployment.png[{product_name_rhdh} deployment on OpenShift,link=self,window=blank]
. Click the URL listed under the *Resources* tab; it will be similar to https://backstage-rhdh-{team-id}.{openshift_cluster_ingress_domain}[https://backstage-rhdh-{team-id}.{openshift_cluster_ingress_domain}^, window="rhdh"]

The sign-in page will be displayed, with the option to log in as a Guest. Click the *Enter* button to use the Guest sign-in.

NOTE: Ignore the GitHub sign-in method, if it's displayed. It is not configured and will not work. 

image:m2/backstage-guest-signin.png[Guest sign-in option on Developer Hub login page,width=60%,link=self,window=blank]


Visit the *Catalog* using the link in the menu on the left-hand side of the {product_name_rhdh} UI. You'll find that the *Kind* dropdown provides only **Plugin** and **Package** options. These represent plugins that can be installed in {product_name_rhdh}, but these don't represent any of the software components deployed. A catalog only delivers value to developers once it’s populated; we’ll address that next.

image:m2/rhdh-empty-catalog.png[Empty {product_name_rhdh} Catalog,link=self,window=blank]